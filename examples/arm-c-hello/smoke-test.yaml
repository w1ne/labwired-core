schema_version: "1.0"
inputs:
  firmware: "./target/firmware"
  system: "./system.yaml"
limits:
  max_steps: 2000000
assertions:
  - uart_contains: "Hello from LabWired C Example!"
  - expected_stop_reason: max_steps
