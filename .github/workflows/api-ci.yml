name: API Fast Checks

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop
    paths:
      - 'foundry/backend/**'
  workflow_dispatch:

defaults:
  run:
    working-directory: ./foundry/backend

jobs:
  test:
    name: go-test-api
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24.0'
          cache-dependency-path: foundry/backend/go.sum

      - name: Run Tests
        run: go test -v -short ./...
