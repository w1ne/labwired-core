# LabWired - Firmware Simulation Platform
# Copyright (C) 2026 Andrii Shylenko
#
# This software is released under the MIT License.
# See the LICENSE file in the project root for full license information.

[package]
name = "labwired-core"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
# build.rs compiles the JIT hot-block WAT into wasm bytes at compile time
# so both the native (wasmtime) and browser (`js_sys::WebAssembly`) backends
# consume identical bytes. See `crates/core/build.rs` and
# `cpu/xtensa_jit/wasm_bytes.rs` (#124 Phase 4 browser-side JIT prototype).
build = "build.rs"

[build-dependencies]
# Used solely by build.rs to pre-compile the hot-block WAT into wasm bytes.
wat = "1.0"
# Used solely by build.rs to compile the native IO-Link bridge C sources when
# the `iolink-native` feature is enabled. Off by default so browser/wasm builds
# never need a C toolchain.
cc = "1.2"

[dependencies]
thiserror = { workspace = true }
tracing = { workspace = true }
serde = { workspace = true }
bitflags = { workspace = true }
labwired-config = { path = "../config" }
anyhow = { workspace = true }
serde_json = { workspace = true }
# bincode 1.3 (no extra wasm-bindgen deps; serde-compatible) — used by the
# `runtime_snapshot` module for binary-format mid-flight resume snapshots.
# The JSON `MachineSnapshot` stays in serde_json for the determinism gates.
bincode = "1.3"
# SHA-256 compression for the ESP32-S3 SHA accelerator model (bootloader
# image-hash verification). Used only by the esp32s3 SHA peripheral.
sha2 = { workspace = true, features = ["compress"] }
# ECDSA for the ATECC608A secure-element component model.
p256 = { workspace = true }
goblin = { workspace = true }
labwired-ir = { path = "../ir" }
labwired-hw-trace = { path = "../hw-trace" }
serde_yaml.workspace = true
memmap2.workspace = true
pio.workspace = true
pio-parser.workspace = true
# Optional: wasmtime is only pulled in when the `jit` feature is enabled.
# The browser crate (`labwired-wasm`) does NOT enable this feature, so
# JS/wasm builds keep their lean dependency tree.
wasmtime = { version = "45", optional = true, default-features = false, features = ["cranelift", "runtime", "std", "wat"] }

[features]
default = []
# Silent-path census (`crates/core/src/census.rs`) — MEASUREMENT ONLY.
#
# Counts three paths on which the simulator can be wrong and still report
# success: dropped Cortex-M memory errors, `StubPeripheral` factory
# fallthrough, and undecoded register offsets inside peripheral `_ => {}` /
# `_ => 0` arms. It records; it never repairs, escalates, or changes control
# flow — with the feature off every recording site expands back to the exact
# code it wraps.
#
# Deliberately NOT in `default` and NOT implied by any other feature, so it
# cannot be switched on as a side effect. Enabling the feature alone still
# writes nothing: a report is emitted only when `LABWIRED_CENSUS_OUT` also
# names an output path at runtime. Build the measuring binary with
# `cargo build -p labwired-cli --features silent-census`.
silent-census = []
# ESP32 WiFi/lwIP functional-outcome thunks (`peripherals::esp32s3::wifi_thunks`
# — canned WL_CONNECTED + simulated sockets). Off by default and NOT in the CI
# feature set (`jit,event-scheduler`), so this fake WiFi state can never compile
# into a production/run build; only the `e2e_labwired_wifi` bring-up harness
# opts in. Run its tests with `--features wifi-thunks`.
wifi-thunks = []
# Egress-bridge transport tests that open real localhost sockets (TCP/MQTT/HTTP).
# Off by default so the standard gate stays hermetic; run with
# `cargo test -p labwired-core --features net-tests`.
net-tests = []
# Enable end-to-end tests that build and run the examples/esp32s3-* firmware
# crates. Requires the +esp toolchain. Off by default so the standard
# workspace `cargo test` does not need the ESP toolchain installed.
esp32s3-fixtures = []
# Phase 3.2 pilot (issue #124): JIT-compile hot Xtensa basic blocks to wasm,
# run via wasmtime native, side-exit on branch. Off by default — the wasm
# build target and the standard test suite both run on the interpreter.
jit = ["dep:wasmtime"]
# Speed plan Phase 2 (#124 follow-on): ISA-agnostic universal-dispatch JIT
# framework scaffold (`crates/core/src/cpu/jit_framework/`). Pure-Rust, no
# extra deps — it carries the shared block cache, side-exit protocol,
# per-ISA frontend trait, native/browser runtime abstraction, interpreter
# fallback, and differential harness, but ZERO per-ISA codegen. Off by
# default so nothing else changes.
jit-framework = []
# Phase 2B.1 (issue #192): event-driven peripheral scheduler. The
# scheduler types (`crates/core/src/sched/`) and the `Peripheral` trait
# extensions compile in unconditionally; this flag toggles the runtime
# drain in `Machine::step`. Off through 2B.N-1; flipped unconditional
# after every peripheral migrates and the legacy walk is deleted.
event-scheduler = []
# Native IO-Link path (plan 2026-06-18): compiles the real `iolinki-master` C
# stack behind a narrow bridge (`native/iolink_master_bridge.*`) into
# `labwired-core`. Off by default and never enabled by the browser crate
# (`labwired-wasm`) so wasm builds stay free of native C and GPL device-stack
# linkage.
iolink-native = []
# MLX90640 round-trip decode test (plan 2026-06-20): compiles the REAL,
# unmodified Melexis MLX90640 driver (third_party/mlx90640-library) behind a
# bridge (native/mlx90640_bridge.*) that routes its I²C reads/writes into the
# LabWired MLX90640 device model. The integration test
# (tests/mlx90640_decode.rs) drives the model and asserts the real driver
# decodes per-pixel °C matching the injected thermal scene. Off by default and
# never enabled by the browser crate so wasm builds need no C toolchain.
mlx90640-decode-test = []
# Batch-width diagnostics (#835): make `plan_cpu_window` record WHICH clause
# bound the CPU quantum, so a board stuck at 1.00 steps/batch names its clamp
# instead of being narrowed by elimination. Costs one thread-local counter
# increment per batch plan, so it is opt-in and never in a shipped build.
# Read it with `labwired_core::quantum_trace::snapshot()`.
quantum-trace = []

[[test]]
name = "mlx90640_decode"
required-features = ["mlx90640-decode-test"]

[dev-dependencies]
labwired-loader = { path = "../loader" }
svd-ingestor = { path = "../svd-ingestor" }
svd-parser = "0.14"

# ── ZERO-TEST TARGETS CANNOT REPORT GREEN ────────────────────────────────────
# Every target below is an integration test whose ENTIRE body sits behind an
# inner `#![cfg(feature = "...")]`. Built without the feature the file compiles
# to an EMPTY libtest binary, which prints `test result: ok. 0 passed` and exits
# 0 — indistinguishable, to CI and to a human, from a suite that ran and passed.
# `required-features` makes cargo SKIP the target instead of reporting it green,
# and makes an explicit `--test <name>` without the feature a hard error.
# Adding a new `#![cfg(feature = ...)]` test file without a block here fails
# `crates/core/src/tests/no_vacuous_test_targets.rs`.

# #![cfg(feature = "event-scheduler")]
[[test]]
name = "bench_walk_free_kw41z"
required-features = ["event-scheduler"]

# #![cfg(feature = "event-scheduler")]
[[test]]
name = "board_batch_width"
required-features = ["event-scheduler"]

# #![cfg(feature = "silent-census")]
[[test]]
name = "census_probe"
required-features = ["silent-census"]

# #![cfg(feature = "silent-census")]
[[test]]
name = "undecoded_register_ratchet"
required-features = ["silent-census"]

# #![cfg(feature = "event-scheduler")]
[[test]]
name = "esp32s3_irq_cache_differential"
required-features = ["event-scheduler"]

# #![cfg(feature = "esp32s3-fixtures")]
[[test]]
name = "e2e_blinky"
required-features = ["esp32s3-fixtures"]

# #![cfg(feature = "esp32s3-fixtures")]
[[test]]
name = "e2e_esp32s3_faithful_boot"
required-features = ["esp32s3-fixtures"]

# #![cfg(feature = "esp32s3-fixtures")]
[[test]]
name = "e2e_esp32s3_flash_boot_no_elf"
required-features = ["esp32s3-fixtures"]

# #![cfg(feature = "esp32s3-fixtures")]
[[test]]
name = "e2e_hello_world"
required-features = ["esp32s3-fixtures"]

# #![cfg(feature = "esp32s3-fixtures")]
[[test]]
name = "e2e_i2c_tmp102"
required-features = ["esp32s3-fixtures"]

# #![cfg(feature = "wifi-thunks")]
[[test]]
name = "e2e_labwired_wifi"
required-features = ["wifi-thunks"]

# #![cfg(feature = "net-tests")]
[[test]]
name = "egress_e2e"
required-features = ["net-tests"]

# #![cfg(all(feature = "event-scheduler", not(debug_assertions)))]
[[test]]
name = "esp32c3_ble_pong_perf_probe"
required-features = ["event-scheduler"]

# #![cfg(feature = "event-scheduler")]
[[test]]
name = "esp32c3_bt_event_residency_gate"
required-features = ["event-scheduler"]

# #![cfg(feature = "event-scheduler")]
[[test]]
name = "esp32c3_clamped_full_state_differential"
required-features = ["event-scheduler"]

# #![cfg(feature = "event-scheduler")]
[[test]]
name = "esp32c3_oled_profile"
required-features = ["event-scheduler"]

# #![cfg(all(feature = "event-scheduler", not(debug_assertions)))]
[[test]]
name = "esp32c3_pms_shipped_firmware"
required-features = ["event-scheduler"]

# #![cfg(feature = "event-scheduler")]
[[test]]
name = "esp32c3_shipped_lab_batch_gate"
required-features = ["event-scheduler"]

# #![cfg(feature = "event-scheduler")]
[[test]]
name = "esp32c3_usb_cdc_console"
required-features = ["event-scheduler"]

# #![cfg(feature = "event-scheduler")]
[[test]]
name = "esp32c3_walk_differential"
required-features = ["event-scheduler"]

# #![cfg(feature = "event-scheduler")]
[[test]]
name = "esp32s3_doom_frame_oracle"
required-features = ["event-scheduler"]

# #![cfg(feature = "event-scheduler")]
[[test]]
name = "esp32s3_smp_yield_latency"
required-features = ["event-scheduler"]

# #![cfg(feature = "event-scheduler")]
[[test]]
name = "esp32s3_walk_differential"
required-features = ["event-scheduler"]

# #![cfg(feature = "iolink-native")]
[[test]]
name = "iolink_native_master"
required-features = ["iolink-native"]

# #![cfg(feature = "event-scheduler")]
[[test]]
name = "kw41z_walk_free_differential"
required-features = ["event-scheduler"]

# #![cfg(feature = "event-scheduler")]
[[test]]
name = "nrf52840_timer_machine_gate"
required-features = ["event-scheduler"]

# #![cfg(feature = "event-scheduler")]
[[test]]
name = "nrf52_easydma_tick512_fidelity"
required-features = ["event-scheduler"]

# #![cfg(feature = "event-scheduler")]
[[test]]
name = "nrf52_timer_walk_differential"
required-features = ["event-scheduler"]

# #![cfg(feature = "event-scheduler")]
[[test]]
name = "nrf54l15_grtc_walk_differential"
required-features = ["event-scheduler"]

# #![cfg(feature = "event-scheduler")]
[[test]]
name = "nrf54l15_idle_ff_speedup"
required-features = ["event-scheduler"]

# #![cfg(feature = "esp32s3-fixtures")]
[[test]]
name = "openai_deck_s3_boot"
required-features = ["esp32s3-fixtures"]

# #![cfg(all(feature = "jit-framework", feature = "jit"))]
[[test]]
name = "riscv_jit_alu_lockstep"
required-features = ["jit", "jit-framework"]

# #![cfg(all(feature = "jit-framework", feature = "jit"))]
[[test]]
name = "riscv_jit_branch_lockstep"
required-features = ["jit", "jit-framework"]

# #![cfg(all(feature = "jit", feature = "event-scheduler"))]
[[test]]
name = "riscv_jit_c3_oled_differential"
required-features = ["event-scheduler", "jit"]

# #![cfg(all(feature = "jit-framework", feature = "jit"))]
[[test]]
name = "riscv_jit_combined_bench"
required-features = ["jit", "jit-framework"]

# #![cfg(all(feature = "jit-framework", feature = "jit"))]
[[test]]
name = "riscv_jit_combined_lockstep"
required-features = ["jit", "jit-framework"]

# #![cfg(feature = "jit-framework")]
[[test]]
name = "riscv_jit_lockstep"
required-features = ["jit-framework"]

# #![cfg(all(feature = "jit-framework", feature = "jit"))]
[[test]]
name = "riscv_jit_mem_lockstep"
required-features = ["jit", "jit-framework"]

# #![cfg(feature = "event-scheduler")]
[[test]]
name = "rp2040_i2c_irq_delivery"
required-features = ["event-scheduler"]

# #![cfg(feature = "event-scheduler")]
[[test]]
name = "rp2040_timer_machine_gate"
required-features = ["event-scheduler"]

# #![cfg(feature = "event-scheduler")]
[[test]]
name = "stm32_dma_walk_differential"
required-features = ["event-scheduler"]

# #![cfg(feature = "event-scheduler")]
[[test]]
name = "stm32_timer_walk_differential"
required-features = ["event-scheduler"]

# #![cfg(feature = "event-scheduler")]
[[test]]
name = "stm32f103_dma_walk_differential"
required-features = ["event-scheduler"]

# #![cfg(feature = "event-scheduler")]
[[test]]
name = "stm32f401_walk_differential"
required-features = ["event-scheduler"]

# #![cfg(feature = "event-scheduler")]
[[test]]
name = "stm32h563_walk_differential"
required-features = ["event-scheduler"]

# #![cfg(feature = "event-scheduler")]
[[test]]
name = "stm32l073_walk_differential"
required-features = ["event-scheduler"]

# #![cfg(feature = "event-scheduler")]
[[test]]
name = "stm32l476_walk_differential"
required-features = ["event-scheduler"]

# #![cfg(feature = "event-scheduler")]
[[test]]
name = "systick_walk_differential"
required-features = ["event-scheduler"]

# #![cfg(all(feature = "event-scheduler", not(debug_assertions)))]
[[test]]
name = "world_esp32c3_ble_pong"
required-features = ["event-scheduler"]
