# Pending Silicon Verification

Standing rule: **every chip-model fix is provisional until verified against real
hardware.** A sim-consistent green in the Tier-1 matrix proves the model is
internally consistent with the fixture; only silicon breaks the circularity.
Each model-behavior change adds an entry here in the same PR; an entry closes
when its hardware verification lands (and the matrix cell graduates to the
silicon-anchored tier with the HIL workstream).

| # | Model change | PR/commit | HW verification recipe | Board | Status |
|---|---|---|---|---|---|
| 1 | Bit-band translation gated on core (M3/M4 only); H5/WBA GPIO un-shadowed | `ee1133c` | New `h563_mmio_diff` oracle (pattern: `l476_mmio_diff`): GPIO word-writes at 0x4202_xxxx — ODR/BSRR/BRR data path on PB0/PF4/PG4 incl. one-word BS-over-BR priority, plus a 4-pattern parity sweep over the GPIOE/GPIOF config surface — sim vs silicon over the AP1 dapdirect recipe (`OpenOcd::spawn_stm32h563`). 8/8 cases + 48/48 register-patterns byte-exact under `H563_STRICT=1`. | NUCLEO-H563ZI | ✅ **silicon-verified 2026-06-11** |
| 2 | T1 shift-immediate flags suppressed inside IT blocks | `60445bd` | Instruction-level oracle: IT-block sequences with T1 LSL/LSR/ASR, APSR captured on silicon (extend `thumb_oracles`) | STM32F103 (bench) | ✅ **silicon-verified 2026-06-08** — `it_block_shift_preserves_flags` passes hw+diff on the bench F103 (PR #191) |
| 3 | Thumb-1 STRH/LDRSB/LDRH/LDRSH register-offset decode | `4ebed86` | Same `thumb_oracles` extension: loaded/stored values + sign-extension vs silicon | STM32F103 (bench) | ✅ **silicon-verified 2026-06-08** — `strh_ldrh_reg_offset` / `ldrsb` / `ldrsh` pass hw+diff (PR #191) |
| 4 | GDMA descriptor-walk mem-to-mem (ESP32-S3) | `fa292bd` | JTAG Unity run on the bench S3: same descriptor sequence on silicon, byte-compare (recipe: `HW_ORACLE_RESULT.md` in the platformio demo) | bench ESP32-S3 (proven setup) | open |
| 5 | ESP32-C3 TIMG0 wired to the real Timg model | `9dfe444` | T0 counter advance + latch semantics on silicon (JTAG or UART-reporting probe firmware) | **ESP32-C3 board availability unconfirmed** — blocked on hardware until then | open (blocked-on-HW) |
| 6 | RV32C compressed-branch decode fix drifted the `riscv_uart_ok` trace fingerprint | `c7148f2` | **Corrected diagnosis.** The nightly `Trace Drift Assertions` red was *not* demo-blinky pacing — the four trace cases never load blinky firmware. The gate (`scripts/trace_drift_assert.sh`) is a **sim-regression snapshot**: it fingerprints sim result+snapshot+UART against committed `examples/ci/fingerprints/`, not a silicon diff. The RVC decoder fix (#7) landed after the baseline (#76) → `riscv_uart_ok` legitimately drifted; the firmware still prints correct `RV OK` and passes. Re-baselined `riscv_uart_ok.sha256` (the only drifting case; the three ARM cases still match). | n/a (sim-only gate) | resolved (re-baselined) |
| 7 | RV32C compressed-branch offset encoding (`C.BEQZ`/`C.BNEZ`/`C.J`) + funct3=4 arithmetic group | `c7148f2` | Run the same RV32C branch/jump sequence on real RISC-V silicon (ESP32-C3 RV32IMC) and compare taken/not-taken outcomes + target PCs against the model | ESP32-C3 (board availability unconfirmed — shares #5's blocker) | open (blocked-on-HW) |
| 8 | Cortex-M shift family (LSL/LSR/ASR/ROR) sets the carry flag — immediate **and** register forms (was N/Z only); register forms also now honour IT-block flag suppression | PR #191 | **Caught BY silicon, not pending it.** Adding xpsr/NZCV diffing to `thumb_oracles` failed `lsrs_immediate` against the bench F103 (sim C=0 vs silicon C=1). Fixed the immediate forms, then the register forms (`LslReg`/`LsrReg`/`AsrReg`), then `Ror` — all three carried the same gap. ADDS/SUBS carry+overflow flags also silicon-anchored. Re-verified — 54/54 hw+diff on the F103. | STM32F103 (bench) | ✅ **silicon-verified 2026-06-08** |
| 9 | F103 WDT (IWDG/WWDG) + USART2 + DMA1 + RTC (CRH/CNT) register reset values | PR #192 | Reset values probed off the bench F103 via OpenOCD, pinned as `stm32f1_mmio_diff` RESET_CASES; sim matches silicon. | STM32F103 (bench) | ✅ **silicon-verified 2026-06-08** |
| 10 | F103 was wired to the L4 calendar-RTC model, not an F1 counter RTC | PR #192 | **Root cause of the CRL=0x2101 discrepancy:** the F1 RTC (CRH/CRL/PRL/CNT counter) is a different peripheral from the L4 calendar RTC (TR/DR/CR/ISR); CRL@0x04 was reading the L4 DR reset (0x2101). New `rtc_f1` model + F103 yaml rewire; CRH/CNT now match silicon for the right reason. | STM32F103 (bench) | ✅ **fixed 2026-06-08** (model) — *remaining:* cold CRL=0 vs idle 0x20 needs a clock-aware RTC to pin across the cold→synced transition |
| 11 | TIM2 (general-purpose timer) update-generation event latches the CCxIF compare-match flags, not UIF alone | PR (exec-oracle) | **Caught BY the new peripheral-execution oracle.** `stm32f1_exec_oracle` runs real ARM code on a full F103 chip bus (sim) and on silicon: enable TIM2 clock, set PSC/ARR, seed CNT non-zero, write `EGR.UG`, read back. Sim said `SR=0x01` (UIF only); silicon said `SR=0x1F` — the UG reload sets CNT=0, which matches every reset-zero CCRx in output-compare mode → CC1..4IF also latch (documented STM32 gotcha). Model now latches compare-match on UG (`!basic`); re-verified hw+diff byte-exact on the bench F103. CNT/ARR/PSC also cross-checked. | STM32F103 (bench) | ✅ **silicon-verified 2026-06-08** |
| 11a | Basic timers (TIM6/TIM7) gated out of the UG compare-match path (`basic: true`) on STM32L073/L476 | PR (exec-oracle) | Config-correct per RM0008 (basic timers have no capture/compare channels, so UG sets UIF only) but **not yet silicon-verified** — no L0/L4 board on the bench. Verify with an L0/L4 over SWD: enable TIM6 clock, `EGR.UG`, read `SR` → expect `0x01`. | STM32L0/L4 (none on bench) | open (config-correct, HW-pending) |
| 12 | GPIO 32-bit word writes go through an atomic `write_u32` (was byte-decomposed) so BSRR honours BS-over-BR priority | PR (exec-oracle) | **Caught BY the oracle.** `gpioa_bsrr_set_reset` writes `BSRR=0x0010_0010` (set pin4 AND reset pin4): silicon's single 32-bit transaction applies BS priority → ODR pin4 set; sim's default byte-decomposition split the set-half (low 16) from the reset-half (high 16) into separate `write_reg` calls, so reset clobbered set → pin4 cleared (sim ODR=0x000F vs hw 0x001F). Added `GpioPort::{read_u32,write_u32}` handing the whole word to `write_reg`. Re-verified hw+diff byte-exact (final ODR=0x001C across BSRR-set/reset + BS-priority + F1 BRR). | STM32F103 (bench) | ✅ **silicon-verified 2026-06-08** |
| 13 | F1 `RCC_AHBENR` reset value 0x14 (SRAMEN + FLITFEN enabled out of reset), was 0 | PR (exec-oracle) | **Caught BY the oracle.** `crc32_two_words` RMW-enables CRCEN; the diff flagged the scratch register holding the AHBENR read-back (sim 0x40 vs hw 0x54 = 0x14 \| CRCEN). RM0008 §7.3.6: AHBENR reset 0x00000014. Set `F1Rcc::new().ahbenr = 0x14`; re-verified hw+diff. (The read-modify-write enable path matters — overwriting AHBENR would clear SRAMEN/FLITFEN and hang a program running from SRAM.) | STM32F103 (bench) | ✅ **silicon-verified 2026-06-08** |
| 14 | DMA1 memory-to-memory engine — **no model change**, validated against silicon | PR (exec-oracle) | First oracle to drive an *autonomous* engine: `dma1_mem_to_mem` arms an 8-bit mem-to-mem copy (CMAR→CPAR, MINC/PINC, MEM2MEM\|EN) then stops; a new sim-only harness knob (`ThumbOracleCase::settle_ticks`) ticks the bus after the program so the DMA completes (one byte/tick), mirroring silicon's concurrent run. Destination bytes, `CNDTR1=0`, and `ISR=0x7` (GIF1\|TCIF1\|HTIF1) match byte-exact on the bench F103 — the existing DMA model was already correct, now silicon-anchored. | STM32F103 (bench) | ✅ **silicon-verified 2026-06-08** |
| 15 | EXTI 32-bit word writes go through an atomic `write_u32` (was byte-decomposed) so SWIER edge-detect + PR rc_w1 behave | PR (exec-oracle) | **Caught BY the oracle (on the sim side, pre-silicon).** `exti_swier_sets_and_clears_pr` word-writes `SWIER=0x5` then `PR=0x1`; the default byte-decomposition read the rc_w1 PR back and re-wrote it, clearing the still-pending line 2 (sim PR=0 vs expected 0x4). Same class as the GPIO BSRR fix (#12). Added `Exti::{read_u32,write_u32}`. | STM32F103 (bench) | ✅ **silicon-verified 2026-06-08** |
| 16 | Clearing an EXTI PR bit (rc_w1) also clears the matching SWIER bit | PR (exec-oracle) | **Caught BY the oracle, silicon-checked before fixing.** After the `write_u32` fix, sim PR matched but SWIER read 0x5 vs silicon 0x4; the bench F103 `_hw` run confirmed silicon de-asserts the software-event line when PR is cleared (RM0008 §10.3.6). Model now clears `swier &= !cleared` in the PR write. Re-verified hw+diff byte-exact. | STM32F103 (bench) | ✅ **silicon-verified 2026-06-08** |
| 17 | AFIO_MAPR masks reserved bits to 0 on read (was stored verbatim) | PR (exec-oracle) | **Caught BY the oracle.** `afio_mapr_reserved_bits_read_zero` writes reserved bits 27 + 21 plus a remap bit; silicon reads the reserved bits back as 0 (`_hw` confirmed), the model returned them set. MAPR implements remap bits [20:0]; [23:21]/[31:27] reserved, SWJ_CFG [26:24] write-only/read-undefined. Masked the store to `value & 0x001F_FFFF`. (The oracle deliberately writes 0 to SWJ_CFG — `0b111` there disables SWD and would drop the debugger.) Re-verified hw+diff byte-exact. | STM32F103 (bench) | ✅ **silicon-verified 2026-06-08** |
| 18 | IWDG PR/RLR are write-protected until KR receives the 0x5555 unlock (was stored unconditionally) | PR (exec-oracle) | **Caught BY the oracle.** `iwdg_pr_rlr_write_protected_without_key` writes PR/RLR with no prior unlock; silicon keeps them at reset (PR=0, RLR=0xFFF — `_hw` confirmed), the model stored the writes. Model now gates PR/RLR on a `write_access` flag set by KR=0x5555 and cleared by any other KR code. *Note:* on real silicon the positive path (latch after unlock) additionally needs the IWDG clock domain running (LSI/started) — not asserted here since the 0xCCCC start key would arm a watchdog reset; the model stores on unlock alone (pragmatic for survival sims). | STM32F103 (bench) | ✅ **silicon-verified 2026-06-08** (negative path) |
| 19 | CRC_IDR is an 8-bit register on STM32F1/F0/L0 (was treated as 32-bit) | PR (exec-oracle) | **Caught BY the oracle.** `crc_idr_is_8bit_on_f1` writes 0x12345678 to CRC_IDR; silicon keeps only the low byte (0x78 — `_hw` confirmed), the model stored the full word. Added a `with_idr_width` flag on the CRC model (default 32-bit for F2+/L4+) + `config: { idr_width: 8 }` on the F103 and L073 yamls. F103 silicon-verified; L073 (L0) is config-correct per RM0367 §14, HW-pending. | STM32F103 (bench) | ✅ **silicon-verified 2026-06-08** (F103; L0 HW-pending) |
| 20 | AFIO EXTICR1..4 mask their reserved upper half [31:16] to 0 (were stored verbatim) | PR (exec-oracle) | **Caught BY the oracle.** `afio_exticr1_upper_half_reads_zero` sets a reserved upper bit + a valid nibble; silicon reads the upper half back as 0 (`_hw` confirmed). Each EXTICR holds four 4-bit fields in [15:0] (RM0008 §9.4.3); masked the four stores to `value & 0xFFFF`. | STM32F103 (bench) | ✅ **silicon-verified 2026-06-09** |
| 21 | GPIO CRL/CRH round-trip + DBGMCU_CR round-trip — **no model change**, validated against silicon | PR (exec-oracle) | Two validation oracles, both byte-exact sim-vs-silicon. `gpioa_crl_crh_round_trip` confirms the 32-bit config registers store verbatim (no reserved fields). `dbgmcu_cr_round_trip` is a **silicon surprise**: RM0008 §31.16.2 marks DBGMCU_CR bits [4:3] reserved, but the bench F103 reads them back as written (0x1F, not 0x07) — they are plain R/W storage here. The model's verbatim store already matches; the oracle pins the real behaviour so a future "mask the reserved bits" change can't silently break it. | STM32F103 (bench) | ✅ **silicon-verified 2026-06-09** |
| 22 | NVIC ISER/ICER set-enable / clear-enable — **no model change**, validated against silicon | PR (exec-oracle) | `nvic_iser_icer_enable_disable` enables IRQ6+IRQ28 via ISER0 then clear-enables IRQ6 via ICER0; ISER0 reads back with IRQ28 still set (`_hw` byte-exact). Wires the Cortex-M block (NVIC @ 0xE000E100, SCB, DWT) into the exec-oracle bus via `configure_cortex_m`, validating the interrupt-**enable** machinery and exposing DWT_CYCCNT for future timed oracles. Interrupt *delivery* (exception entry/exit) is the next step — see `roadmap.md` P1. | STM32F103 (bench) | ✅ **silicon-verified 2026-06-09** |
| 23 | Interrupt **delivery** — full exception entry/exit path validated against silicon (roadmap P1) | PR (exec-oracle) | `exti0_interrupt_delivery`: executed code relocates VTOR to a `[vector table][ISR][main]` program, unmasks EXTI line 0, enables IRQ6 in the NVIC, clears PRIMASK, and software-triggers the line. The CPU vectors to the ISR (entry + register stacking), which sets a RAM marker + clears EXTI_PR, then `BX LR` returns (unstacking) to `main`. Marker set + EXTI_PR cleared, byte-exact on the bench F103 (`_hw`/`_diff`). Harness additions: `Thumb::Data` (vector words), `bx`/`cpsie_i` encoders, per-case `entry_offset`, and opt-in `live_peripherals` (post-step `tick_peripherals_fully` → `set_exception_pending`). **No model bug** — the NVIC/SCB/exception path was already correct; now anchored. *Limitation:* sim level-source re-pend is tick-granular, so non-idempotent ISRs may run an extra time vs silicon — fine for the marker ISR. | STM32F103 (bench) | ✅ **silicon-verified 2026-06-09** |
| 24 | DWT cycle counter mechanism (enable → count → read) — **no model change**, validated against silicon (roadmap P2) | PR (exec-oracle) | `dwt_cyccnt_advances` enables DEMCR.TRCENA + DWT_CTRL.CYCCNTENA, resets CYCCNT, reads it back and records the self-relative boolean `CYCCNT != 0`. The absolute count diverges (sim is **not** cycle-accurate — its DWT advances one tick per executed instruction; silicon counts real core cycles), so only the boolean is invariant and the divergent register is cleared before the snapshot. Pins the DWT timing *mechanism*, not cycle fidelity (out of scope for this sim). | STM32F103 (bench) | ✅ **silicon-verified 2026-06-09** |
| 25 | Firmware-level conformance differential (roadmap P5 v1) — found peripheral access/latch latency gaps | PR (conformance) | `firmware-f103-conformance` drives every peripheral and writes a digest; `f103_conformance.rs` runs the same ELF on the full-chip `Machine` and on silicon and diffs. First run 10/11 (91%). Found gaps the micro-oracles can't: **CRC reset latency** (feeding DR right after CR.RESET lost a word on silicon) and **TIM2 status-latch latency** (SR read 0 one instruction after EGR.UG). Correct firmware (a `settle()` between the hazardous accesses) fixes both; the sim is lenient because it models no peripheral access/latch latency. Residual `exti_pr` is a firmware-context HW artifact (the isolated EXTI oracle confirms the sim value is correct). | STM32F103 (bench) | ✅ **pipeline silicon-proven 2026-06-09** (10/11; latency-modeling is a documented sim limitation) |
| 26 | STM32H5 SPI v3 layout (`stm32h5` profile): CFG1 reserved mask, SPE config-lock, mode-fault machinery (MASTER refused + MODF latch on SS-low), CTSIZE mirror, TXTF/IFCR, TXC restore | h563-class-models | **Verified at birth** — every behavior was bench-probed BEFORE modeling (capture corpus probe-20260611), then re-proven by `h563_class_diff` (14 SPI cases byte-exact, `H563_STRICT=1`). Sim TX engine (EOT on TSIZE exhaustion) is spec-derived: the bench part had no spi_ker_ck, so real frame movement is yaml-documented as a divergence. | NUCLEO-H563ZI (bench) | ✅ **silicon-verified 2026-06-11** (register surface; data path sim-only by documented divergence) |
| 27 | ADC L4-layout power-up handshake: ADEN with ADVREGEN up (DEEPPWD clear) raises ISR.ADRDY | h563-class-models | `h563_class_diff` ADC sequence (DEEPPWD reset → clear → ADVREGEN → ADEN→ADRDY) byte-exact vs the bench H563. The same model serves L476 — its L4 silicon re-check rides the existing L476 oracle backlog. | NUCLEO-H563ZI (bench) | ✅ **silicon-verified 2026-06-11** (H563; L476 cross-check pending) |
| 28 | RTC v3 model (`rtc_v3`): WPR key gate, INIT→INITF, BYPSHAD, INITS-from-year, calendar BCD tick, locked-write drop | h563-class-models | Bench-probed before modeling (incl. the BYPSHAD=0 shadow-stale surprise, kept as a documented simplification); re-proven by `h563_class_diff` (9 RTC cases byte-exact) — including the silicon-sticky RTCSEL write-once note. 1 Hz tick approximated at 32768 sim ticks/second. | NUCLEO-H563ZI (bench) | ✅ **silicon-verified 2026-06-11** |
| 29 | GPDMA model (8ch, software-request mem-to-mem): TCF/HTF/IDLEF, BNDT drain, SAR/DAR advance, EN auto-clear, CFCR w1c, RESET-does-not-clear-flags | h563-class-models | The autonomous engine was driven ON SILICON over SWD (real SRAM→SRAM block copy) and the same flow is replayed by `h563_class_diff` (8 cases incl. destination words byte-exact). Linked-list mode + peripheral-request mode not modeled (documented). | NUCLEO-H563ZI (bench) | ✅ **silicon-verified 2026-06-11** |
| 30 | Advanced timers: CC5IF/CC6IF (SR bits 16/17) latch with the channel-1..4 compare path; compare-match also latches while COUNTING (was UG-only) | h563-class-models | Bench TIM1 PWM run read SR=0x0003001F (UIF+CC1..4IF+CC5/6IF) — the run-time compare and the internal channels 5/6 were both missing from the model. `h563_class_diff` TIM1 sequence byte-exact. F103-family UG semantics unchanged (existing oracles still green). | NUCLEO-H563ZI (bench) | ✅ **silicon-verified 2026-06-11** |
| 31 | H563 irq class: NVIC declared in the chip yaml; fixture delivery round-trip | h563-class-models | NVIC ISER/ICER enable machinery diffed on the M33 (`h563_class_diff`, 3 cases); full exception entry/exit was already silicon-anchored on the bench F103 (#23). | NUCLEO-H563ZI (bench) | ✅ **silicon-verified 2026-06-11** (enable surface; delivery anchored via #23) |
| 32 | Timer: IRQ level follows CC1..4IF&CC1..4IE (was UIF&UIE only); wrap also latches compare flags | foreign-fw twin | **Caught by foreign firmware** (embassy time driver arms CCR1 alarms with CC1IE). Bench TIM2 probe: CC1IF + CC1IE pends the NVIC line (ISPR1 bit 13) with the CPU halted; capture7 pins the same on TIM12, embassy's actual time-driver pick. | NUCLEO-H563ZI (bench) | ✅ **silicon-verified 2026-06-11** |
| 33 | PWR H5 layout (`PwrH5`): VOSCR/VOSSR voltage scaling, DBPCR, register surface | foreign-fw twin | Bench probe capture5: resets (PMCR=0x0C, VOSSR=0x2008, SCCR=0x100, VMSR bit 20) + VOS transitions Scale3→0→2→3 read VOSSR 0x2008/0xE008/0x6008/0x2008 — model formula `(VOS<<14)|ACTVOSRDY|VOSRDY` is the silicon behavior. | NUCLEO-H563ZI (bench) | ✅ **silicon-verified 2026-06-11** |
| 34 | FLASH H5 layout: ACR latency/WRHIGHFREQ/PRFTEN read-back; interface regs only | foreign-fw twin | Bench probe capture6: ACR reset 0x13, round-trips 0x11/0x23/0x02 verbatim; OPTCR=1, NSCR=1, OPTSR_CUR pinned as representative. Program/erase not modeled (documented). | NUCLEO-H563ZI (bench) | ✅ **silicon-verified 2026-06-11** |
| 35 | TIM12 wired (embassy `time-driver-any` pick) | foreign-fw twin | Bench probe capture7: resets (ARR=0xFFFF, rest 0), PSC/CCR1/DIER round-trips, free-running CNT under the debugger. | NUCLEO-H563ZI (bench) | ✅ **silicon-verified 2026-06-11** |
| 36 | v2-USART TXEIE mask bug: was bit 3 (TE!) — every TX-enabled v2 UART held a phantom interrupt | foreign-fw twin | **Caught by foreign firmware** (embassy enables the USART3 NVIC line; the phantom pend looped in its default handler — invisible to all polling fixtures). Bench probe capture8: CR1=FIFOEN\|TE\|RE\|UE pends nothing; +TXEIE (bit 7) pends ISPR1 bit 28. Mask fixed to bit 7 + regression test. | NUCLEO-H563ZI (bench) | ✅ **silicon-verified 2026-06-11** |
| 37 | Cortex-M: LDAEX/STLEX/LDA/STL (+B variants) implemented; CLREX→no-op; pending-exception bitmask widened 64→256 (IRQ 120 = exception 136 was silently dropped) | foreign-fw twin | **Caught by foreign firmware**: Rust atomics on thumbv8m compile to acquire/release forms the decoder routed to a silent skip — embassy's executor run-queue never woke; then its TIM12 IRQ (120) overflowed the u64 pending mask. End-to-end behavioral anchor: the same unmodified embassy ELF (usart_vcp) now produces a **byte-identical 22-line UART stream on the bench board and in the sim** (hello + 20 timer-paced ticks + DONE). Instruction-level thumb-oracle cases for the LDAEX family on the M33 remain open. | NUCLEO-H563ZI (bench) | ✅ **behaviorally silicon-anchored 2026-06-11** (instruction oracle open) |
| 38 | DBGMCU wired at 0x44024000 (embassy pokes DBGMCU_CR during init) | foreign-fw twin | IDCODE 0x10016484 from the 2026-06-10 capture; CR is plain R/W storage (cf. #21 F103 finding). | NUCLEO-H563ZI (bench) | ✅ **silicon-pinned 2026-06-11** |
| 39 | FLASH H5 ACR mask 0x133 → 0x13F (LATENCY bits 3:2 were dropped — HAL's latency-5 read-back at 250 MHz failed) | foreign-fw twin (Cube) | **Caught by ST's own HAL example.** Bench capture9: ACR write 0x25 reads 0x25, write 0x13F reads 0x13F. My capture6 probes only used latency ≤ 3 — the mask derivation missed the upper bits. | NUCLEO-H563ZI (bench) | ✅ **silicon-verified 2026-06-11** |
| 40 | GPDMA per-channel NVIC routing (`irq_base`, channel n → line 27+n) + window 1KB → 2KB (channel 7's CTR1/CBR1/CSAR/CDAR live above 0x400) + MISR per-channel flag&enable mirror | foreign-fw twin (Cube) | **Caught by ST's HAL example** (uses channel 7, IRQ 34; `HAL_DMA_IRQHandler` gates on MISR which read 0). Bench capture10: ch7 TCIE pends ISPR1 bit 2, transfer byte-exact. Bench capture11: MISR bit 7 = flags&enables live (drops on TCIE clear, returns on re-enable, clears with CFCR). | NUCLEO-H563ZI (bench) | ✅ **silicon-verified 2026-06-11** |
| 41 | GPDMA data-handling engine: SDW/DDW width conversion via PAM (zero-pad / sign-extend / left- / right-truncate) + SBX / DBX / DHX exchanges (`DmaUnitTransform` on `DmaRequest`) | foreign-fw twin (Cube) | The seven expected-result vectors from ST's DMA_DataHandling example are encoded as unit tests, and the **unmodified example ELF passes all 7 checks on the sim AND on the bench board** (LED1 success-blink, error LED dark, observed live over SWD). | NUCLEO-H563ZI (bench) | ✅ **silicon-verified 2026-06-11** (same-ELF twin) |
| 42 | GPDMA linked-list mode: node fetch from {CLBAR[31:16], CLLR.LA}, per-U-flag register update (UT1/UT2/UB1/USA/UDA/UT3/UB2/ULL), list-start with CBR1=0, per-block TCF, EN-clear only at list end | foreign-fw twin (Cube) | Bench capture12 first: a raw 2-block chain over SWD pinned the full lifecycle (both blocks byte-exact, CLLR→0, BNDT=0, CSR=0x301, CCR keeps TCIE with EN cleared). Model fetches nodes as memory→own-register DMA copies. Then ST's DMA_LinkedList example (3-node queue, HAL_DMAEx) reaches its success loop on sim AND board — same ELF, LEDs observed live over SWD. | NUCLEO-H563ZI (bench) | ✅ **silicon-verified 2026-06-12** |
| 43 | Cortex-M TBB/TBH: branch-table base for Rn=PC is the unaligned in-execution PC (insn+4) — the old Align(PC,4) read tables 2 bytes early at 2-mod-4 addresses | foreign-fw twin (Cube) | **Caught by ST's HAL**: `HAL_DMA_RegisterCallback`'s TBB switch dispatched every callback ID into the same slot, so the error callback overwrote the completion callback. Fix per ARMv7-M A7.7.185/186; behavioral anchor = the DMA_LinkedList same-ELF twin. The three ARM `Trace Drift Assertions` fingerprints legitimately drifted (corrected branch paths) and were re-baselined with all behavioral assertions still green — same precedent as #6. | NUCLEO-H563ZI (bench) | ✅ **behaviorally silicon-anchored 2026-06-12** |
| 44 | FDCAN model (H5 fixed message-RAM layout): CCCR INIT/CCE protocol, TEST.LBCK loopback TX→RX FIFO0 through SRAMCAN, IR rc_w1 + TC-gated-by-TXBTIE, TXFQS/RXF0S index machinery, RXF0A ack semantics, CONFIG-block identity (VERR/SIDR) | h563-fdcan (tier1 can class) | **Capture-first** (capture13, 2026-06-12): full internal-loopback frame driven over SWD pinned the map (0x84 = XIDAM — the fixed layout, NOT generic Bosch RXF0C), CCE-clears-with-INIT, IR = RF0N\|TFE with TC gated by the per-buffer TXBTIE enable, RXF0A advancing F0GI rather than blanking the status. Model rebuilt from the capture (the stranded eb04139f draft had IR at 0x080 colliding RXGFC/TXBC at 0xC0 and generic-Bosch FIFO-config registers that do not exist on H5). Re-proven by `h563_class_diff`: 22 FDCAN cases, 65/65 total match. tier1 row now 13/13 with the `can` beachhead class (fixture check_can replays the same sequence). Acceptance filtering, bit timing, FDCAN2 and IT1/ILS routing not modeled (yaml-documented). | NUCLEO-H563ZI (bench) | ✅ **silicon-verified 2026-06-12** |
| 45 | GDMA peripheral-coupled mode (ESP32-S3): PERI_SEL routing, UART/UHCI0 + SPI2/3 + I2S0/1 coupled byte movement, owner/LEN descriptor writeback (IN unconditional; OUT gated on OUT_AUTO_WRBACK, OUT_CONF0 bit 2), auto-complete fallback for AES/SHA/ADC/RMT/LCD_CAM | feat/gdma-peripheral-coupling | Same recipe as #4 (JTAG Unity on the bench S3): drive an ESP-IDF DMA driver (uart / spi_master / i2s) on silicon, replay the descriptor sequence in sim, byte-compare buffers + INT_RAW ordering; highest-value probes are the I2S RXEOF_NUM byte semantics and the UART FIFO-idle EOF policy | bench ESP32-S3 (proven setup) | open |

Notes:
- Recipes reuse existing machinery only: hw-oracle mmio-diff replays, `thumb_oracles`,
  the S3 JTAG Unity loop, F103 capture scripts. No new harnesses required.
- Entries #6/#7 are the live example of the rule working — a model fix (#7) tripped a
  downstream regression snapshot the moment it merged. The original #6 mis-attributed
  it to a firmware pacing change; clearing the nightly is what surfaced the true cause.
- The `Trace Drift Assertions` gate compares sim-vs-committed-sim fingerprints, so
  re-baselining after a *verified, intentional* model fix is correct snapshot
  maintenance, not a silicon claim — the silicon obligation lives in #7.
