#!/bin/bash
#
# Compatibility wrapper: keep legacy entrypoint but run the canonical hook script.
#

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/pre-commit.sh"
