# Override: commit pre-built fixture binaries so CI can run oracle tests
# without requiring the ESP toolchain.
!*.bin
!*.elf
!*.s
!*.ld
!Makefile
!README.md
# Intermediate object files are not needed
*.o
