# Chips that ship but that NO CI gate executes firmware on, through the CLI.
#
# Enforced by scripts/ci/chip_coverage.py, which never reads this file to decide
# what IS covered — coverage is derived from what the workflows actually run.
# This file only carries the gaps, so it cannot be used to claim a chip is fine.
#
# The rules the gate enforces on this file:
#   * a shipped chip with no CLI proof and no entry here FAILS the gate;
#   * an entry for a chip that has since become covered FAILS (stale excuse);
#   * an entry for a chip that no longer ships FAILS (stale row);
#   * `max_uncovered` must EQUAL the number of entries, so closing a gap forces
#     the ceiling down and it can never drift back up quietly.
#
# `kind:`
#   pending — the path exists (model, fixture, example) and the work is wiring
#             it into a gate. This is debt with a route out.
#   waived  — no CLI path exists at all today, with the concrete reason.
#
# Same shrink-only contract as KNOWN_GAPS in crates/core/tests/board_coverage_ratchet.rs
# and SMOKE_LESS_ALLOWLIST in crates/core/tests/strict_onboarding.rs. Do NOT add
# a row to turn a red gate green — that re-opens the hole the gate exists to close.

max_uncovered: 3

uncovered:
  esp32s3-zero:
    kind: pending
    reason: >-
      NOT a missing fixture — re-diagnosed 2026-08-14. tests/fixtures/tier1/esp32s3.elf
      runs on this descriptor and prints all eight TIER1 classes PASS, looping to
      --max-steps, under `labwired run --chip configs/chips/esp32s3-zero.yaml`.
      The blocker is that a CLI cell uses `labwired test`, and that path cannot
      boot a flash-resident Xtensa image: the same ELF and chip stop at
      `memory_violation` after ~25k instructions, because the flash-XIP windows
      (flash_icache @0x42000000, flash_dcache @0x3c000000) are never populated
      the way `run --chip` populates them via configure_xtensa_esp32s3. This
      affects the WHOLE S3 family, not this board — the parent esp32s3 fails
      identically — which is why no S3 chip has a CLI cell. Fixing the test
      path's Xtensa boot closes this entry and unblocks a family-wide cell;
      writing another ELF does not.

  nrf5340:
    kind: pending
    reason: >-
      Boots a real Zephyr ELF (tests/fixtures/nrf5340-zephyr-hello.elf) in
      firmware_survival.rs, but in-process — nothing drives it through the CLI.
      configs/systems/nrf5340dk.yaml and the ELF are both committed, so this
      needs only an io-smoke.yaml plus a coverage-matrix cell.

  rp2350:
    kind: pending
    reason: >-
      crates/firmware-rp2350-demo builds, and rp2350_clkrst_profile.rs pins the
      reset path, but there is no system manifest or smoke script for it — so
      no script can be pointed at a gate yet. Needs examples/rp2350-*/ with a
      system.yaml + io-smoke.yaml, then a matrix cell.
