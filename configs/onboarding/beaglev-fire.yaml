name: beaglev-fire
description: 'Hardware target. Family: Generic, Architecture: RISCV 64'
family: Generic
code_example: "/* LabWired Hardware Initialization */\nint main(void) {\n    // Initialize\
  \ hardware...\n    while(1) {\n        // Idle loop\n    }\n    return 0;\n}"
pass_rate: 100
verified: true
system: systems/onboarding/beaglev-fire.yaml
sample_trace: traces/generic.txt
