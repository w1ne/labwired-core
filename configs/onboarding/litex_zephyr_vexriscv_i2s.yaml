name: litex_zephyr_vexriscv_i2s
description: 'Hardware target. Family: Generic, Architecture: RISCV 32'
family: Generic
code_example: "/* LabWired Hardware Initialization */\nint main(void) {\n    // Initialize\
  \ hardware...\n    while(1) {\n        // Idle loop\n    }\n    return 0;\n}"
pass_rate: 100
verified: true
system: systems/onboarding/litex_zephyr_vexriscv_i2s.yaml
sample_trace: traces/generic.txt
