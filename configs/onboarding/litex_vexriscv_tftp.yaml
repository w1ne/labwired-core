name: litex_vexriscv_tftp
description: 'LiteX SoC with VexRiscv configured to boot over network via TFTP. Architecture:
  RISC-V RV32IMA.'
family: LiteX
url: https://github.com/enjoy-digital/litex
code_example: "/* LabWired Hardware Initialization */\nint main(void) {\n    // Initialize\
  \ hardware...\n    while(1) {\n        // Idle loop\n    }\n    return 0;\n}"
pass_rate: 100
verified: false
system: systems/onboarding/litex_vexriscv_tftp.yaml
sample_trace: traces/generic.txt
validation:
  method: local-structural
  reason: structural-ok (non-ARM32)
  checks:
    system_manifest: true
    chip_descriptor: true
    flash_base_present: true
    ram_base_present: true
  timestamp_utc: '2026-03-12T23:52:28.629197+00:00'
  run_url: ''
  artifacts_url: ''
